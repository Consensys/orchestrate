version: '3.7'

services:
  report:
    build:
      context: .
        # environment:
        # REPORT_metadata_orchestrate-dev-env: "v0.1.1"
      # REPORT_metadata_orchestrate-e2e: "v0.1.0"
    volumes:
      - ./output:/use/src/app/in
      - ./output:/use/src/app/out

